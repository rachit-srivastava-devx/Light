# SOW — Fleet build session 2026-09-02

Base commit: `e942446` (checkpoint of prior in-progress work). Read [`../../FLEET-LEARNINGS.md`](../../FLEET-LEARNINGS.md)
first — hard boundaries (never touch fleet-rs, git-mutex commits) apply here.

## Track A — fix the 9 documented voice-pipeline defects (priority, highest confidence)

Source: `docs/REVIEW-2026-08-06-END-TO-END-FLOW.md`. Each defect below is a worktree + branch
`fix/<slug>` off `e942446`. **Contract first**: each worker writes/extends the failing test named
below BEFORE touching implementation, confirms it's RED against `e942446`, then makes it pass.
Verify gate: `npm run verify` (boundary-lint → typecheck → vitest → pytest → cargo test) must be
green, plus a manual drive of the affected path (voice/relay = run `smoke:realtime`; UI = orb-shape
or ux-gate script) with evidence saved under `evidence/fleet-2026-09-02/<slug>/`.

| # | Slug | Defect | File(s) | Acceptance test |
|---|---|---|---|---|
| A1 | no-hardcoded-transcript | App launch injects a transcript before real mic/STT completes | `apps/mobile/src/AppController.ts:27-45` | New test asserts no transcript is set until a real STT event arrives; boot with STT mocked-to-silence must show empty transcript |
| A2 | listening-state-delivery-receipt | "Listening" UI reflects local mic only, no relay/STT ack | `App.tsx`, `RelayClient.ts`, `relay-rs/src/main.rs` | Test: UI listening-state only flips to `confirmed` on a relay delivery-receipt message, not on local mic-open |
| A3 | ws-reconnect-timeout | WebSocket errors swallowed; no reconnect, no open-timeout | `RelaySocket.ts`, `RelayClient.ts` | Test: socket that never opens within N ms surfaces a typed timeout error; a mid-session close triggers exactly one reconnect attempt with backoff |
| A4 | audio-bed-fallback-on-provider-failure | Provider failure kills the voice loop instead of a presence-preserving fallback | `relay-rs/src/session.rs` | Test: forced provider error still emits a fallback audio-bed event, session stays alive |
| A5 | streaming-stt | STT is batch-at-end, blueprint requires streaming partials | Sarvam/Deepgram adapters in `backend/voice-provider-sidecar/` | Test: adapter emits ≥1 partial transcript event before the final, for a multi-second fixture |
| A6 | relay-async-io-timeout | Rust relay blocks on provider calls with no timeout, can stall the connection | `backend/relay-rs/src/` | Test: a provider call that hangs past timeout returns a typed error, connection continues serving other sessions |
| A7 | tts-true-streaming | TTS fully buffered before send despite a "chunked" protocol | TTS send path (find via `grep -r chunked backend/`) | Test: first audio chunk is sent before the full utterance finishes synthesizing (assert on chunk arrival timing, not just chunk count) |
| A8 | barge-in-wiring | Barge-in can't interrupt an in-flight provider call; 100ms yield constant unwired | wherever the yield constant lives (grep `100ms` / barge-in code) | Reuse/extend existing `apps/mobile/src/voice/BargeInPlaybackLatency.test.ts` — assert an in-flight provider call is actually cancelled, not just that playback stops |
| A9 | no-fake-tts-default | Default provider config is `fake` unless explicitly overridden; fake TTS bytes playable as audible speech with no marker | Rust relay + sidecar provider config | Test: booting with no explicit provider config fails closed (typed config error), does not silently default to `fake` |

**Conflict map (merge order, not build order — build all in parallel worktrees):**
A2/A3 both touch `RelayClient.ts`/`RelaySocket.ts` — merge A3 first, rebase A2 on top.
A5/A6/A7 all touch the relay/provider pipeline — merge in numeric order, rebase the rest.
Everything else is file-disjoint.

## Track B — Speed-of-Thought P0 (Orb-side only, design first, no fleet-rs)

Per `FLEET-LEARNINGS.md` known-gap: build only the Orb-side loop (build-mode second mode reusing
existing `ConversationPort` + FSM, freeze protocol, `lld-ready` gate, thin status echo) and stop at a
**frozen, written `lld.v1` handoff contract** — do not fabricate a fleet-rs PR step. One lead-architect
pass writes the contract + acceptance tests before any builder touches code. Lower priority than
Track A this session; picked up after Track A's first wave lands.

## Process (every worker, every tool)

1. `git worktree add ../adhd-focus-orb-worktrees/<slug> -b fix/<slug> e942446` (from inside this repo).
2. Write the acceptance test, confirm RED, implement, confirm GREEN, run full `npm run verify`.
3. Manual drive per the L8 gate — screenshot/log evidence saved, not just "tests pass."
4. Append an entry to `../../FLEET-LEARNINGS.md` (what worked, any invocation gotcha).
5. Commit inside the worktree only (never the shared main checkout) — no concurrent commits without
   the git-mutex pattern if two workers ever share a worktree (they shouldn't here, 1 worktree/slug).
6. Report back: files changed, test output (real, pasted), verify exit code, evidence path.
