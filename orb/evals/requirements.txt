# Track E (anti-mirage) — simulated-user + judge tooling.
# Installed into evals/.venv ONLY (python3.12 -m venv .venv). Never installed into
# backend/relay-py/.venv — that venv belongs to other concurrent tracks.
#
# Reuse decision (owner directive 2026-08-28: check for a readymade solution before building):
# both of these are purpose-built OSS simulated-user harnesses identified in
# RESEARCH-simulated-user-and-voice-ux.md. Do not hand-roll a conversation simulator.

deepeval>=4.2.0          # Apache-2.0. confident-ai/deepeval. Verified on PyPI: 4.2.0, 2026-08-24.
                          # ConversationSimulator + ConversationalGolden — the text-loop simulated user.
langwatch-scenario>=1.3.0  # Apache-2.0. langwatch/scenario. Verified on PyPI: 1.3.0, 2026-08-18.
                            # AgentAdapter/UserSimulatorAgent/JudgeAgent — same API carries over to the
                            # voice loop later (ElevenLabs/OpenAI-Realtime/Pipecat/Gemini-Live adapters).

# Judge model wrappers — there is no OpenAI key in this environment; both DeepEval's
# DeepEvalBaseLLM and Scenario's LiteLLM-routed default model need a custom wrapper over
# Gemini or Anthropic instead.
# google-genai (NOT the older/legacy "google-generativeai") is Google's current unified Gemini
# SDK. Verified on PyPI: google-genai 2.20.0 (2026-08-25, active); google-generativeai's latest
# is 0.8.6 (2025-12-16) and is the SDK Google is migrating callers away from.
google-genai>=1.0
anthropic>=0.26

python-dotenv>=1.0
requests>=2.32
pytest>=8.3
