[package]
name = "fleet-scan"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0.20"
fleet-types = { path = "../fleet-types" }

[dev-dependencies]
serde_json = "1.0"
