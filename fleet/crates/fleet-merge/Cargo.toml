[package]
name = "fleet-merge"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2.0.20"
fleet-types = { path = "../fleet-types" }
tokio = { version = "1.35", features = ["sync", "fs", "process"] }
futures = "0.3"

[dev-dependencies]
tempfile = "3.27.0"
