[package]
name = "integrate"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
fleet-merge = { path = "../fleet-merge" }

[dev-dependencies]
tempfile = "3"
