[package]
name = "router"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
types = { path = "../types" }

[dev-dependencies]
serde_json = "1.0.151"
