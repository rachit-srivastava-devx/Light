[package]
name = "fleet-router"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
fleet-types = { path = "../fleet-types" }

[dev-dependencies]
serde_json = "1.0.151"
