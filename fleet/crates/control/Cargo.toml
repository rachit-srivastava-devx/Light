[package]
name = "control"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
fleet-govern   = { path = "../fleet-govern" }
fleet-lifecycle = { path = "../fleet-lifecycle" }
