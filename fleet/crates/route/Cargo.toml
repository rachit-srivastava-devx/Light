[package]
name = "route"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
petgraph = { version = "0.6", optional = true }
router = { path = "../router" }
