[package]
name = "fleet-govern"
version = "0.1.0"
edition = "2021"

[dependencies]
fleet-types = { path = "../fleet-types" }
fleet-router = { path = "../fleet-router" }
thiserror = "2.0.20"
tiktoken-rs = "0.6.0"
fs4 = { version = "0.9", features = ["sync"] }

[dev-dependencies]
tempfile = "3"
