[package]
name = "verify"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde     = { version = "1", features = ["derive"] }
thiserror = "2"
types = { path = "../types" }
tempfile    = "3"
include_dir = "0.7"
