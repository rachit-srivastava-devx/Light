[package]
name = "verify"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
blake3    = "1"
serde     = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
types = { path = "../types" }
tempfile    = "3"
include_dir = "0.7"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
