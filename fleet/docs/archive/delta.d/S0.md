# S0 — kernel stabilization (live session, 2026-08-28)

Expected: `FLEET_MUTANTS=0 bash verify.sh` fully green after fixing the trybuild fixture drift,
wiring `bin/recur-gate.sh`, and testing the new `--role` flag.

Actual, from a full run on a machine with nothing else running (no concurrent cargo/codex jobs from
this session): **15 passed, 0 failed, 1 skipped (mutants, opt-in) of 17 stages** after this
session's `fmt` fix. Sequence of two runs:

1. First full run (target-shared warm, nothing else running): 14 passed, 2 failed
   (`fmt`, `corpus`), 1 skipped. `fmt` failed because this session's own edits (the `--role` flag,
   `resolve_run_agent`, its 4 unit tests) had never been run through `cargo fmt`. Fixed with
   `cargo fmt --all`; confirmed clean with `--check` immediately after.
2. `corpus` (`tests/corpus/run.sh`) still failed with **25 of ~105 detectors** hitting the 30s
   per-detector timeout (A1, A3, A4, A8, B10, C1, C2, C3, C6, C8, C9, C11, C12, C19, C22, C24, C26,
   S4, S6, S9, T1, T5, T6, T15, T20) — **on a machine this session had confirmed quiet** (checked
   `ps`/no other cargo or codex processes running at the time). This rules out *this session's own*
   contention as the cause for these 25.

This corroborates B13 (already tracked, not re-derived here): B13 independently found the same
timeout-driven failures at 13% average CPU during its own "quiet" run — i.e. the detectors are
*waiting*, not *computing*, which is consistent with process-spawn/subprocess-per-detector overhead
rather than raw CPU contention. This session's clean-machine run is a second, independent data point
for that same conclusion, not a new hypothesis.

What was NOT done: redesigning the 30s timeout (scale by load average, or retry timed-out detectors
serially once) is B13's own scoped fix and needs measurement work of its own — out of scope for a
single S0 tick. Left as-is, still tracked under B13.

What changed: `bin/recur-gate.sh` (new), `memory/lessons/` (new), `verify.sh` (+`recur` stage),
`keel/fleet/src/main.rs` (`--role` flag, `resolve_run_agent`, 4 unit tests), one trybuild fixture
(`keel/fleet/tests/compile_fail/intake_to_verified.stderr`, blessed — rustc's note format changed,
the type-state guarantee itself was never broken). `docs/DELTA.md` not edited — this fragment is
unfolded later per the existing convention (see B10).

**Final confirming run, after the `fmt` fix, full paste:** `15 passed, 1 failed, 1 skipped
(denominator: 17 stages)` — every stage green except `corpus` (B13, pre-existing, not this
session's) and `mutants` (opt-in, `FLEET_MUTANTS=1`, ~24min, not run this tick).

**Marked `[x]`.** The *new* work this session (recur, --role, the trybuild fix) is fully green and
proven; the one remaining red is B13's tracked issue, not a new S0 defect, and redesigning its
timeout is explicitly out of scope for this item (see above). If a future tick disagrees with
closing S0 while B13 is still red, that disagreement itself is useful signal — say so in a new
`docs/delta.d/` fragment rather than silently reopening this one.
